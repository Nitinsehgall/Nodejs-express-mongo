to start the node app
just go the folder by doing cd cd
and go to that file 
and if you want to run the file then write--------node index.js(file name)
or you can use the nodemon package-nodemon index.js(file name )