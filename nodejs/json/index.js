const bioData={
    name:'nitin',
    age:23,
    class:'mca'
}

console.log(bioData);

const jsonData =JSON.stringify(bioData)
console.log(jsonData);
const prse=JSON.parse(jsonData)
console.log(prse);



